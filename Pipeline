from scrapy.exceptions import DropItem

class bballpipeline(object):
    def process_item(self, item, spider):
        if not(item["WL"]):
            raise DropItem()
        else:
            return item

#        if(item["POS"]):
#            item["POS"] = item["POS"][-1:]
#        else:
#            return item

#        if(all(item.values(""))):
#            item.values = "0"
#        else:
#            return item
